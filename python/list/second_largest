# top  find thew second largest in list
list=[]
num=int(input("enter:"))
for i in range(num):
    list.append(int(input("enter the no. in list:")))
print(list)
list.sort(reverse=True)
print(list)
print("second largest no. in list is:",list[1])    
